# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: mysql2
  encoding: utf8
  database: raco1
  username: mysql.raco1
  password: raco1.mysql
  host: raco1
  port: 3306

development:
  <<: *default

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default

production:
  <<: *default

pg:
  adapter: postgis
  encoding: utf8
  database: gps
  username: gps
  password: Track.all.the.things!
  host: gps-db
  port: 5432